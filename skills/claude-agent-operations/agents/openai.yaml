interface:
  display_name: "Claude Agent Operations"
  short_description: "Route Fable and Claude agents with clear ownership"
  default_prompt: "Use $claude-agent-operations to plan efficient parallel agent work for this task."
policy:
  allow_implicit_invocation: true
